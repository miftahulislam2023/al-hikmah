export default function SearchForm() {
    // Temporary disabled - need to implement with new schema
    return (
        <div className="container mx-auto p-6">
            <h1 className="text-2xl font-bold mb-6">Student Search</h1>
            <p className="text-gray-600">This component needs to be reimplemented with the new user schema.</p>
        </div>
    );
}